Use status to inspect the current palace state.

- Check palace path, drawer counts, and configured wings.
- Use it before debugging search issues so you know whether data is actually present.
- Pair it with `search` or `repair` when counts look wrong.
