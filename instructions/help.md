MemPalace keeps AI context searchable.

- `init` sets up the palace and entity registry.
- `mine` ingests project files or transcripts.
- `search` retrieves relevant drawers.
- `status` summarizes what is stored.
- `hook run` and `instructions` support automation-oriented workflows.
