Initialize MemPalace for a new workspace.

- Confirm the project directory you want to bind.
- Decide which people, projects, and aliases should seed the entity registry.
- Review ambiguous names before accepting them.
- After init, run `mempalace mine <dir>` to populate the palace.
